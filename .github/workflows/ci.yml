name: CI Build

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: mlugg/setup-zig@v1
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
      
      - name: build
        run: cargo run